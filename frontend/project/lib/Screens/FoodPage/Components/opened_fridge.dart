class OpenFridge extends StatefulWidget {
  const OpenFridge({Key? key}) : super(key: key);

  @override
  _OpenFridgeState createState() => _OpenFridgeState();
}

class _OpenFridgeState extends State<OpenFridge> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
